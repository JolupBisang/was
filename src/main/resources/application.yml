spring.profiles:
  active: local
  group:
    local: local, common, secret
    blue: blue, common, secret
    green: green, common, secret

server:
  env: blue

---
spring:
  config.activate.on-profile: local
#  security.oauth2.client.registration.google:
#    redirect-uri: http://localhost:8080/api/login/temp

server:
  port: 8080
  address: localhost

---
spring:
  config.activate.on-profile: green
#  security.oauth2.client.registration.google.redirect-uri: http://3.34.4.158/api/login/temp

server:
  port: 8080
  address: 3.34.4.158

---
spring:
  config.activate.on-profile: blue
#  security.oauth2.client.registration.google.redirect-uri: http://3.34.4.158/api/login/temp

server:
  port: 8081
  address: 3.34.4.158

---

spring:
  config.activate.on-profile: common
  application.name: demo
  datasource:
    url: jdbc:mysql://localhost:3306/JolupBisang?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
